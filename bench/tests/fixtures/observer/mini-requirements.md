# Mini Requirements — Notification Retry Policy (fixture)

Seeded checklist for testing the PRD-complete session-end trigger. Not a real
project requirements file — consumed only by the observer spike.

- [ ] Retry policy defined (backoff strategy + max retries)
- [ ] Table schema finalized
- [ ] Tests written
